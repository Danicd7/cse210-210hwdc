using System;

class Goal
{
    private string goalName, goalDescription, points;

    public Goal (string _goalName, string _goalDescription, string _points)
    {
        goalName = _goalName;
        goalDescription = _goalDescription;
        points = _points;
    }
}